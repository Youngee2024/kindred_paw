# Server-only Paystack secret. Configure this in Vercel; never prefix it with VITE_.
PAYSTACK_SECRET_KEY=sk_test_replace_me

# Public URL used for the Paystack callback.
APP_URL=http://localhost:3000

# Keep false for presentation mode. Set true only after Paystack is configured.
VITE_ENABLE_LIVE_PAYMENTS=false
